<h1>Editar</h1>

<%= form_for @figure do |f| %>

<div>
	<%= f.label :name %>
	<%= f.text_area :name %>
</div>
<div>
	<%= f.label :description %>
	<%= f.text_area :description %>
</div>
<div>
	<%= f.submit %>
</div>

<% end %>